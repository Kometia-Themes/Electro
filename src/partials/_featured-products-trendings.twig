
{% if settings.featured_collection_trending is not empty and collections[settings.featured_collection_trending] is defined and collections[settings.featured_collection_trending].products | length > 0 %}
  {% set featured_collection = collections[settings.featured_collection_trending] %}
  <section class="featured-products prod-grid__4-ext">
    <div class="container">
      <h2 class="text-center">{{ settings.feature_products_trending_title }}</h2>
      <hr class="hr-small">
      {% for products_row in featured_collection.products | batch(4) %}
        <div class="row">
        {% for product in products_row %}
          {% include '_product-item.twig' %}
        {% endfor %}
        </div>
      {% endfor %}
    </div>
  </section>
{% else %}
  <section class="featured-posts-home content-placeholder">
    <div class="container">
      <h2 class="text-center">{{ settings.feature_products_trending_title }}</h2>
      <hr class="hr-small">
      <div class="row">
      <div class="placeholder-helper text-center">
        <div class="col-xs-12 placeholder-info">
          <p class="lead">Esta sección mostrarán los productos de la colección.</p>
          <p class="text-center"><a href="/admin/collections" class="btn btn-success">Gestionar colecciones</a>
      <a href="/admin/themes" class="btn btn-success">Ir a sección de temas</a></p>
        </div>
      </div>
      </div>
    </div>
  </section>
{% endif %}
